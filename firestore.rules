rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow anyone to read or write any document under "series/"
    match /series/{seriesId} {
      allow read, write: if true;
    }
  }
}
